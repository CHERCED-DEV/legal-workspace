## Comprobaciones que pasan limpias

- **(1) Uniformidad AC-04 en los ocho documentos del foco:** `03` (§0.5 tabla + §11.6/§11.10), `04` (§3.4 CHECK + §10 C1), `05` (§11.2 + tabla §6.9), `06` (§2.7 + §5.4 + §11 pregunta 2), `09` (§3.1 fila 10, §3.4, §8.2, §16.18), `12` (§2.3, §3.1, §6.2, §7), ADR-004 (b)1 y ADR-009 (§ decisión + pendiente 3) declaran de forma coincidente "en la lista cerrada, **sin productor en v0**, preservación = conducta por defecto y estado derivado". PASA.
- **(5) AT-008:** `12` §3.1 y §2.3 afirman preservación observable en `pending` y **niegan explícitamente** el rótulo ("Afirmar el rótulo hoy sería probar una capacidad que el diseño no produce"). PASA.
- **(6) Payload sin productor y asimetría:** solo `09` §3.2 contrata `ProposalPreservedForReconciliationP`, y `09` §3.4 declara la asimetría frente a `FactWithdrawn` con su razón ("su use case existe y solo se discutía si escribe"). PASA.
- **(3) parcial:** `04` §3.4 `TABLE proposals` no tiene columna de estado agregado (comentario explícito), `02` §4 `Proposal` no tiene campo `status`, y `03` §10.7 prohíbe el booleano `preserved_for_reconciliation`. PASA en el corpus técnico v0 (excepción en `vertical-slice-v0.md`, hallazgo 4).

## Hallazgos

**1 · ALTA — `docs/technical-design/v0/00-technical-kernel.md` §8.1 (línea 310)**
Cita: «**Declarados sin productor en v0:** `FactWithdrawn` (conservado para no reabrir el contrato…)». La lista cerrada del kernel enumera nueve tipos + `FactWithdrawn` y **omite por completo** `ProposalPreservedForReconciliation` (cero menciones en todo el kernel). Problema: la comprobación (4) **falla** — ADR-004 (b)1 y `09` §3.1 declaran once tipos idénticos; el kernel declara diez. Es el defecto raíz que sus hermanos siguen señalando como abierto: `04` §10 C1 lo etiqueta «**POR VERIFICAR** (reconciliación pendiente en un documento superior)… el kernel §8.1 todavía no lo enumera en esa segunda categoría»; `06` §5.4 opción 1: «Requiere corregir la lista del kernel §8.1, no el ADR»; `09` §3.1 y §3.4 lo describen como «que el kernel omite» / «Ausente del kernel §8.1». Corrección: en §8.1 sustituir por «**Declarados sin productor en v0:** `FactWithdrawn` y `ProposalPreservedForReconciliation` (enmienda AC-04 aprobada; patrón idéntico — la preservación es conducta por defecto y estado derivado, no almacenado)», y retirar entonces el `POR VERIFICAR` de `04` §10 C1.

**2 · ALTA — `docs/architecture/adrs/ADR-008-proposal-and-human-authorization-model.md` §Preguntas pendientes, punto 2 (línea 166)**
Cita: «**DECISIÓN PENDIENTE — `ProposalPreservedForReconciliation`.** Figura en la lista cerrada de eventos de ADR-004 y no en la del kernel §8.1… Opción recomendada: conservarlo en la lista declarado sin productor en v0». Problema: la comprobación (2) **falla**; el punto 1 de esa misma lista **sí** fue actualizado («RESUELTA — Aritmética de revisiones, enmienda AC-02»), lo que hace la omisión asimétrica y no atribuible a un documento entero sin tocar. Además la línea 181 repite «Dos puntos abiertos con él: la aritmética de revisiones (pregunta 1) y `ProposalPreservedForReconciliation` (pregunta 2)», cuando ambos están cerrados. Corrección: reescribir el punto 2 como «**RESUELTA — enmienda AC-04 aprobada**: permanece en la lista cerrada de ADR-004 (b)1, sin productor en v0, patrón `FactWithdrawn`; preservación = conducta por defecto y estado derivado» y corregir la línea 181 a cero puntos abiertos con ADR-004.

**3 · ALTA — `docs/technical-design/v0/16-open-implementation-decisions.md` §5 (línea 247)**
Cita: «**`ProposalPreservedForReconciliation`** (C1): ¿evento, columna de estado, o conducta derivada? … Recomendación registrada: opción 3 … **Debe ratificarse antes del primer caso real**». Problema: el registro de decisiones pendientes sigue listando como no ratificado algo que AC-04 ratificó (`04` §10 C1: «La opción 3 fue la aprobada (AC-04): cero cambios de esquema»). Un implementador que consulte el índice de pendientes concluirá que el punto sigue abierto. `16` no contiene ninguna mención de AC-04. Corrección: retirar la fila de §5 y registrarla en la sección de decisiones ya cerradas, citando AC-04 y `09` §8.2.

**4 · ALTA — `docs/architecture/vertical-slice-v0.md` líneas 457–458, 324, 197**
Cita (escenario normativo del slice): «2. La Proposal se PRESERVA en estado PRESERVED_FOR_RECONCILIATION / **(evento ProposalPreservedForReconciliation)**. El trabajo NO se descarta.» Problema: afirmación **vigente**, no histórica, de que el evento **se emite** — comprobación (1) falla. Agrava: línea 324 contrata `Proposal { … status ∈ PENDING | APPROVED | REJECTED | SUPERSEDED | PRESERVED_FOR_RECONCILIATION }` como campo del agregado, y línea 197 repite el ciclo de vida con estado almacenado — comprobación (3) falla fuera de `technical-design/v0`. El documento tiene **cero** menciones de AC-04 pese a ser la fuente que `04` §10 y `09` §8.2 citan como «escenario normativo». Corrección: en el resultado 2 eliminar el paréntesis del evento y escribir «la Proposal se preserva por **ausencia de mutación**; el rótulo `PRESERVED_FOR_RECONCILIATION` es derivado y su evento queda **sin productor en v0** (AC-04)»; borrar `status` de la línea 324 con nota de remisión a `06` §2.7; corregir la línea 197 al vocabulario derivado.

**5 · ALTA — `docs/domain/glossary.md` §11 Proposal, líneas 592–593 y 576**
Cita (diagrama *Lifecycle preliminar*): «`--conflicto de revisión--> PRESERVED_FOR_RECONCILIATION [ProposalPreservedForReconciliation + condición REVISION_CHANGED]`». Problema: el glosario —vocabulario canónico— sigue afirmando que el conflicto **emite** el evento y que el rótulo es alcanzable; comprobación (1) falla. El mismo bloque **sí** incorporó AC-02 con nota explícita («ENMIENDA AC-02 aprobada»), luego la omisión de AC-04 es asimétrica y no es cita histórica marcada. La línea 576 mantiene además el enum de estados de la Proposal sin señal de que sean derivados. Corrección: en el diagrama, cambiar la arista a «`--conflicto de revisión--> [cero mutaciones; REVISION_CHANGED]  ← el evento ProposalPreservedForReconciliation queda SIN PRODUCTOR en v0 (ENMIENDA AC-04 aprobada); el rótulo es derivado y no se exhibe en v0`»; anotar la línea 576 con la remisión al vocabulario único de `06` §2.7.

**6 · MEDIA — `docs/technical-design/v0/11-ux-condition-catalog.md` §3.6, línea 316**
Cita: «El rótulo agregado `PRESERVED_FOR_RECONCILIATION` es **derivado, nunca almacenado**… y hoy **sin productor en v0** (**DECISIÓN PENDIENTE C1**)». Problema: la conclusión es correcta pero la atribuye a una decisión pendiente ya cerrada; comprobación (2) falla. `11` no menciona AC-04 en ningún punto. Corrección: sustituir «(DECISIÓN PENDIENTE C1)» por «(**enmienda AC-04 aprobada**; `04` §10 C1 RESUELTO, `09` §8.2)».

**7 · MEDIA — `docs/technical-design/v0/08-case-context-projections.md` línea 1206 (y §5.4, línea 689)**
Cita: «4. **Resolución de C1 de `04`** (representación de `PRESERVED_FOR_RECONCILIATION`): **sigue abierta en `04`**, pero acotada por la enmienda AC-04…». Problema: `04` §10 C1 está titulado «**RESUELTO — enmienda AC-04 aprobada**» y declara «cero cambios de esquema»; la dependencia declarada de `08` apunta a un conflicto inexistente. La línea 689 arrastra lo mismo en presente («`04` §10 **C1** deja abierto cómo se representa…»), aunque §691 lo corrige acto seguido. Corrección: reescribir el punto 4 como «C1 **cerrado** en `04` por AC-04: sin columna de estado, valor admitido en el `CHECK` sin productor; esta proyección no depende de nada más» y poner la línea 689 en pasado.

**8 · MEDIA — `docs/architecture/boundaries.md` línea 74 y `docs/architecture/adrs/ADR-005-human-authority.md` líneas 129 (inv. 7), 176 (fila 6) y 93**
Citas: boundaries «Proposal **preservada en `PRESERVED_FOR_RECONCILIATION`**»; ADR-005 inv. 7 «`REVISION_CHANGED` … + Proposal preservada»; fila 6 «Proposal **en `PRESERVED_FOR_RECONCILIATION`**». Problema: ambos afirman como estado alcanzable un rótulo que `06` §2.7 declara «computable pero **sin productor** — ninguna Proposal lo exhibe en v0». Ninguno de los dos documentos menciona AC-04 (ADR-005 sí fue actualizado por AC-01/AC-02, luego no es un documento congelado). Un implementador que lea el invariante 7 buscará dónde se escribe ese estado. Corrección: reformular a «la Proposal se **preserva por ausencia de mutación** y sigue visible en `get_case_context(pending)`; el rótulo `PRESERVED_FOR_RECONCILIATION` es derivado (`06` §2.7) y su evento queda sin productor en v0 (AC-04)». En boundaries, la misma línea arrastra además `seq == revision`, superado por AC-02.

**9 · MEDIA — `docs/technical-design/v0/12-testing-strategy.md` (obligación de verificación no recogida)**
`09` §3.4 y §16.18 contratan una obligación sobre la estrategia de pruebas: «**ninguna ruta puede emitirlo** y **un test de superficie debe comprobarlo**» / «Queda como trabajo de verificación que **ninguna ruta lo emita**». Problema: `12` no contiene ningún test —ni en los trece `AT`, ni en la matriz de invariantes, ni en §6.2— que compruebe que ninguna ruta de v0 escribe `ProposalPreservedForReconciliation`; `12` solo declara que `AT-008` **no** afirma el rótulo. La honestidad de método de `09` («"declarado sin productor" es distinto de "no implementado"… es una declaración verificable») queda sin verificador. Corrección: añadir en `12` un test de superficie (junto a los de manifiesto/clases) que recorra el Case Event Log tras la batería completa y asegure `count(event_type IN ('ProposalPreservedForReconciliation','FactWithdrawn')) = 0`, referenciado desde `09` §3.4.

**10 · BAJA — `docs/technical-design/v0/05-mcp-contract.md` §6.9, línea 655**
Cita: `proposal_status_derived: ProposalDerivedStatus; // … PENDING | PARTIALLY_COMMITTED | RESOLVED | PRESERVED_FOR_RECONCILIATION`. Problema: el contrato de salida MCP expone un valor de enum que en v0 es **inalcanzable por construcción**, sin declararlo en el punto de uso (`08` §5.4 sí lo declara para su proyección: «`status_derived` **nunca** toma ese valor y `counters.proposals_preserved` es siempre `0`»). Riesgo menor de que un cliente ramifique sobre un caso que nunca ocurre. Corrección: añadir al comentario «`PRESERVED_FOR_RECONCILIATION` **no se produce en v0** — su predicado depende de un evento sin productor (AC-04, §11.2)».