# Los scripts de shell se ejecutan en Git Bash: con CRLF fallan con "\r: command not found".
*.sh text eol=lf

# Y la plantilla compilada, por la misma razon y con una peor: es un ARTEFACTO.
# El 2026-09-22 llego a master con 68 retornos de carro metidos por un checkout
# de Windows, y eso la separa para siempre de lo que produce la compilacion:
# `test_pagina_publicada.py` no puede volver a cuadrar, y una guarda que esta
# roja por algo que nadie sabe arreglar es una guarda que se aprende a ignorar.
# Se fijan tambien las FUENTES, para que el proximo commit no lo repita.
*.html          text eol=lf
tools/pagina-despacho/src/*  text eol=lf

# La pagina de voces (SPEC-15): lo mismo. `test_pagina_voces.py` compara las
# huellas de sus fuentes y de la plantilla, y un checkout de Windows que les
# meta retornos de carro las separaria para siempre.
tools/pagina-voces/*       text eol=lf
tools/pagina-voces/src/*   text eol=lf

# Y el resto de lo que entra en las huellas de la pagina de oir y marcar.
tools/pagina-despacho/*.mjs              text eol=lf
tools/pagina-despacho/package.json       text eol=lf
tools/pagina-despacho/package-lock.json  text eol=lf
tools/pagina-despacho/vite.config.js     text eol=lf
tools/pagina-despacho/HUELLAS.json       text eol=lf

# Las paginas del proyecto (SPEC-16): con CRLF de un checkout de Windows, los parches y
# las huellas dejan de cuadrar (paso el 2026-09-24).
tools/paginas-del-proyecto/**  text eol=lf
